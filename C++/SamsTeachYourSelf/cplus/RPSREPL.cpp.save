#include <iostream>
#include <cstdlib>
#include <ctime>
#include <map>
std::string opponentPick(void){
	srand(time(NULL));
	int random = rand() % 100;
	int thresholdP =random % 2;
	int thresholdR =random % 3;
	int thresholdS =random % 5;
	std::string results;
	if(thresholdP == 0){
	results = "p";
	}
	else if (thresholdR == 0){
	results = "r";
	}
	else if (thresholdS == 0){
	results = "s";
	}
	return results;
}

int compare(std::string cpu, std::string user){
	std::cout << cpu << " and " << user << std::endl;
	std::map<std::string, std::string> winMap;
	winMap[std::string("r")] = std::string("s");
	#include <cstdlib>
	return 0;
}

int main(void){
	std::cout << "\nWelcome to Rocks Paper Scizzors \n\nKey Controls: \n r: rock \n p: paper \n s: scissors \n q: quit \n" << std::endl;
	while(bool continuous = true){
		std::string userInput;
		int win = 0;
		int loss = 0;
		std::cout << "Player 1: " << win << " | CPU: " << loss << std::endl;
		std::cin >> userInput;
		std::string q ("q");
		if (userInput.compare(q) == 0){
			return 0;
		}else{
			std::string results = opponentPick();
			int winLossResults = compare(results, userInput);
		}
	}
}
