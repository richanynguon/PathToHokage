#include <iostream>
#include <cstdlib>
#include <string>
#include <ctime>

int opponent(){
	srand(time(NULL));
	return rand() % 100;
}

int main(){
	std::cout << "Welcome to Rocks Paper Scizzors \n Key Controls: \n r: rock \n p: paper \n s: scissors \n q: quit" << std::endl;
	while(bool continuous = true){
		string input
		std::cin >> input'		if (input == "q"){
			return 0;
		}
		int results = opponent();
		
		std::cout <<  results << std::endl;
		return 0;
	}
}
