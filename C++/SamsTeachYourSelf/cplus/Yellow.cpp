// hash include imports iostream is the preprocessor directive
#include <iostream>

// initialize function main that return an integer value
int main(){
	/*std - standard namespace, cout (c-out), endl (end line) can
	be found in the std namespace
	:: is defining the scope so count from std scope
	>> extraction operator will extract data or fill data
	 */
	std::cout << "Yellow World" << std::endl;
	// return 0 for success -1 for failure is standard
	return 0;
}
